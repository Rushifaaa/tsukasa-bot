import { Message } from 'discord.js';

const git = (args: string[], msg: Message) => {
    msg.reply("Do you want new commands? Do you need Support? Do you need something else? -> https://github.com/Rushifaaa/tsukasa-bot");
}
export default git;